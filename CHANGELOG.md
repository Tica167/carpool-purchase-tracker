# 變更紀錄 (CHANGELOG)

> 由 prd-sdd-studio skill 自動維護。最新紀錄在最上方。

## 2026-09-24 05:20 — 車主可修改車資（含生效日期）＋ 整月一鍵標記付款

- **做了什麼**：
  - 新增 `CarpoolRate` 資料表（`effective_date`, `amount`），車主可設定「從某天開始每趟車資變成多少」；新登記的共乘紀錄依登記日期對應當時生效的費率計算金額，**已登記過的舊紀錄金額不會被追溯修改**
  - `services.py` 新增 `get_effective_carpool_amount()`、`set_carpool_rate()`；`set_carpool_slot()` 改用動態費率取代原本寫死的 `CARPOOL_AMOUNT`
  - 車主頁面頂部新增「修改車資」表單（金額＋生效日期），僅車主可用
  - 付款狀態卡片改成**可點擊的按鈕**（僅本人查看自己時）：一鍵把當月「所有」共乘紀錄的付款狀態一起設成已付款（或再點一次取消，改回未付款），不需要每天分別點；車主頁面看到的是各成員最新的整月結果
  - 新增 `services.py` 的 `toggle_month_carpool_payment_status()`、`app.py` 路由 `/dashboard/carpool-rate/save`（僅車主）與 `/dashboard/carpool/pay-month`
  - 已在瀏覽器實測：車主把車資從 $30 改成 $50（生效日期今天）→ 頂部「目前每趟車資」立即變 $50；Tina 新登記一趟共乘正確用 $50 計算；Tina 一鍵標記整月已付款後，「本月尚未結算」提示消失，車主頁面即時看到 Tina 已付款
- **為什麼**：使用者反饋逐日標記付款太麻煩，希望結算後一鍵確認整月金額即可；車資也需要能因應實際油價/過路費調整，且不影響已經記錄過的歷史金額
- **影響檔案**：`models.py`, `services.py`, `app.py`, `templates/dashboard.html`
- **Git**：⏸ 未版控
- **文件同步**：✅ 已於下一筆同步（PRD.md、SDD.md 都已更新）

## 2026-09-24 05:35 — 同步 PRD/SDD：車資費率調整、整月一鍵付款

- **做了什麼**：PRD.md 更新 FR-002（依生效費率計費）、FR-005（共乘改為整月一鍵標記）、新增 FR-012（車主可調整車資費率）與對應驗收標準、8.1 補上「車資費率」資料實體，文件版本 1.4 → 1.5；SDD.md §3 補上 `get_effective_carpool_amount()`/`set_carpool_rate()`/`toggle_month_carpool_payment_status()`，§4.2 新增 `carpool_rates` 資料表，§5.3 更新付款狀態按鈕與車資調整表單說明，§6 新增兩個路由，§7 新增非車主改費率的錯誤處理，文件版本 1.9 → 2.0，對應 PRD 版本更新為 1.5
- **為什麼**：讓文件反映車資可調整（不追溯）與付款狀態改為整月一鍵標記的最新規則
- **影響檔案**：`PRD.md`, `SDD.md`
- **Git**：⏸ 未版控
- **文件同步**：✅ 本筆即為文件同步

## 2026-09-24 04:40 — 頂部結算區新增「付款狀態」卡片

- **做了什麼**：
  - `services.py` 新增 `get_month_carpool_payment_status(member_id, year, month)`：本月只要有任何一筆共乘未付款就回傳 False（顯示「未付款」），全部付清或本月沒有紀錄則回傳 True（顯示「已付款」）
  - 一般成員自己頁面：頂部從兩欄變成三欄，新增「付款狀態」卡片（已付款＝綠色文字，未付款＝紅色文字）
  - 車主頁面：每位一般成員的那一行從兩欄變成三欄，同樣新增「付款狀態」，即時反映該成員自己最新標記的結果
  - 已在瀏覽器實測：Tina 已付款顯示綠色、Blue 未付款顯示紅色；Blue 自己標記已付款後，車主頁面立即看到 Blue 的狀態變成「已付款」
- **為什麼**：使用者希望能一眼看出誰付清了、誰還沒付，車主也要能看到成員自己更新後的最新狀態
- **影響檔案**：`services.py`, `app.py`, `templates/dashboard.html`
- **Git**：✅ 已 commit `e5d2c32`
- **文件同步**：✅ 已於下一筆同步（SDD.md 已更新）

## 2026-09-24 04:55 — 同步 SDD：付款狀態卡片

- **做了什麼**：SDD.md §3 補上 `get_month_carpool_payment_status()`，§5.3 更新頂部結算區說明（三欄、付款狀態判斷邏輯），§6 API 新增對應函數，文件版本 1.8 → 1.9
- **為什麼**：讓文件反映頂部新增的付款狀態卡片
- **影響檔案**：`SDD.md`
- **Git**：✅ 已 commit `e5d2c32`
- **文件同步**：✅ 本筆即為文件同步

## 2026-09-24 04:10 — 頂部結算區改版：車主看四位成員總覽、一般成員精簡為單行兩欄

- **做了什麼**：
  - 車主（Hugo）登入後，頂部固定顯示 4 位一般成員（Tina/Blue/Mango/Rennie，依成員名單順序）的「共乘趟數」與「車資本月應付總計」，一行兩欄、共 4 行；**不受下拉切換查看對象影響**（切到 Tina 時頂部仍是四人總覽，下方日曆/代買才切換成 Tina 的內容）
  - 一般成員自己的頁面，頂部精簡成一行兩欄：「共乘趟數」「車資本月應付總計」，移除原本的「車資小計」「代買小計」卡片（代買已經在下方依發起人拆分小計，不需要在頂部重複）
  - `app.py` 新增 `member_ride_summaries`（車主專用，逐一呼叫 `get_member_monthly_summary()` 算出每位一般成員的趟數與車資小計）
  - 已在瀏覽器實測：車主頁面四行資料正確（Tina/Blue 各 1 趟 $30、Mango/Rennie 0 趟 $0）；切換查看對象後頂部四行不變；一般成員（Blue）自己頁面正確顯示單行兩欄
- **為什麼**：使用者確認代買小計已經在下方依發起人拆分顯示，頂部不需要重複；車主需要的是「每個人要付多少車資」的總覽，不是自己的車資（車主不用付錢給自己）
- **影響檔案**：`app.py`, `templates/dashboard.html`
- **Git**：✅ 已 commit `eef19fd`
- **文件同步**：✅ 已於下一筆同步（SDD.md 已更新）

## 2026-09-24 04:20 — 同步 SDD：頂部結算區改版

- **做了什麼**：SDD.md §5.3 更新儀表板頂部說明（車主固定 4 行成員總覽、一般成員單行兩欄、隱私提示規則），§3 補充 `get_member_monthly_summary()` 在車主登入時的呼叫方式，文件版本 1.7 → 1.8
- **為什麼**：讓文件反映頂部結算區改版後的實際行為
- **影響檔案**：`SDD.md`
- **Git**：✅ 已 commit `eef19fd`
- **文件同步**：✅ 本筆即為文件同步

## 2026-09-24 03:20 — 代買刪除、各發起人小計拆分、車資應付總計獨立

- **做了什麼**：
  - `services.py` 新增 `delete_purchase_record()`（僅發起人可刪除，含品項與分攤一起刪除）、`get_month_purchase_subtotal_by_initiator()`（各發起人本月代買小計）、`get_month_payable_by_initiator()`（登入者本月應付給各發起人的金額，分已付/未付）
  - `app.py` 新增 `/dashboard/purchase/<id>/delete` 路由；`/dashboard` 改組出「各發起人小計」「我應付給各發起人（已付/未付）」兩份明細
  - `dashboard.html`：代買紀錄卡片加上「刪除」按鈕（僅發起人本人看得到）；底部原本單一「本月代買小計（我發起的）」拆成兩張表格：「各發起人本月代買小計」（列出每位發起人的小計，例如 Hugo：$100）、「我本月應付給各發起人」（每位發起人分「已付款」「未付款」兩欄，依登入者自己的付款狀態統計）
  - 「本月應付總計」改名為「車資本月應付總計」，數值改成只算共乘車資（`carpool_subtotal`），不再把代買金額算進去
  - 已在瀏覽器實測：Hugo 發起代買分攤給 Tina/Blue → 各發起人小計正確顯示 Hugo:$100；Tina 應付 Hugo 未付$50，標記已付款後正確變成已付$50/未付$0；Hugo 刪除自己發起的紀錄後，該筆品項與分攤全部消失、小計歸零；車資本月應付總計只反映共乘趟數金額
- **為什麼**：使用者希望代買紀錄輸入錯誤時能刪除、能看到欠每個人多少錢（分已付/未付方便對帳），並希望「應付總計」只代表車資（避免跟代買金額混在一起造成誤解）
- **影響檔案**：`services.py`, `app.py`, `templates/dashboard.html`
- **Git**：✅ 已 commit `0ca17a6`
- **文件同步**：✅ 已於下一筆同步（PRD.md、SDD.md 都已更新）

## 2026-09-24 03:40 — 同步 PRD/SDD：代買刪除、發起人小計、車資應付總計

- **做了什麼**：PRD.md 更新 FR-006（代買刪除說明與驗收標準）、FR-005（彙總顯示與車資應付總計業務規則），文件版本 1.3 → 1.4；SDD.md §3 補上 `delete_purchase_record()`/`get_month_purchase_subtotal_by_initiator()`/`get_month_payable_by_initiator()`、§5.3 更新統計卡片與代買區塊說明（刪除按鈕、兩張彙總表格）、§6 API 新增刪除路由、§7 新增非發起人刪除的錯誤處理，文件版本 1.6 → 1.7，對應 PRD 版本更新為 1.4
- **為什麼**：讓文件反映代買可刪除、依發起人拆分小計、應付總計改為僅車資的最新規則
- **影響檔案**：`PRD.md`, `SDD.md`
- **Git**：✅ 已 commit `0ca17a6`
- **文件同步**：✅ 本筆即為文件同步

## 2026-09-24 02:40 — 請假/居家狀態彙總顯示

- **做了什麼**：
  - `services.py` 新增 `get_month_all_day_status(year, month)`，回傳當月所有成員的請假/居家狀態
  - `app.py` 的 `/dashboard` 依身份組出彙總顯示規則：車主查看自己的頁面時，日曆上直接列出「所有人」當天的請假/居家狀態（不用切下拉逐個看）；一般成員查看自己的頁面時，除了自己的狀態，也會自動看到「車主」的狀態（因為車主請假/居家會影響大家能不能共乘）；透過下拉切換查看別人時，維持只顯示該成員的狀態
  - `dashboard.html`／`base.html`：日曆格子改成可顯示多筆「成員：狀態」文字（格子高度改為最小高度、可隨內容增高，不會被裁切）
  - 已在瀏覽器實測：車主自己頁面同時看到 Hugo/Tina/Mango 三人的狀態；一般成員（Rennie）自己頁面只自動看到 Hugo 的請假，看不到 Tina/Mango 的
- **為什麼**：使用者希望車主的請假/居家能讓大家提前在自己頁面就看到（不用特地去查），車主自己也希望能一次總覽所有人狀態，不用一個一個切換查看
- **影響檔案**：`services.py`, `app.py`, `templates/dashboard.html`, `templates/base.html`
- **Git**：✅ 已 commit `3767385`
- **文件同步**：✅ 已於下一筆同步（PRD.md、SDD.md 都已更新）

## 2026-09-24 02:55 — 同步 PRD/SDD：請假/居家彙總顯示規則

- **做了什麼**：PRD.md 4.11 補上彙總顯示子功能與 2 條驗收標準，文件版本 1.2 → 1.3；SDD.md §3 補上 `get_month_all_day_status()`、§5.3 更新日曆彙總規則說明、§6 API 更新對應函數，文件版本 1.5 → 1.6，對應 PRD 版本更新為 1.3
- **為什麼**：讓文件反映車主狀態自動出現在他人頁面、車主自己頁面彙總看全部人狀態的規則
- **影響檔案**：`PRD.md`, `SDD.md`
- **Git**：✅ 已 commit `3767385`
- **文件同步**：✅ 本筆即為文件同步

## 2026-09-24 02:20 — 本機與雲端成員編號對齊

- **做了什麼**：發現本機資料庫的成員 ID（1, 4, 5, 6, 7，因先前測試時新增/刪除過成員而跳號）跟雲端 Neon 資料庫的 ID（1, 2, 3, 4, 5，全新建立）不一致。確認本機當時無任何歷史紀錄後，清空本機 `carpool_purchase.db` 並重新用 `init_db()` 建立，現在本機與雲端的成員編號完全一致（1=Hugo, 2=Tina, 3=Blue, 4=Mango, 5=Rennie）。已在瀏覽器驗證本機正常運作
- **為什麼**：編號不一致會讓之後對照本機/雲端資料、或手動查資料庫時容易搞混（測試雲端功能時就因此抓錯人）
- **影響檔案**：本機 `carpool_purchase.db`（資料庫檔案本身被 `.gitignore` 排除，不進 git）
- **Git**：➖ 不適用（資料庫檔案不受版控）
- **文件同步**：➖ 不適用

## 2026-09-24 02:00 — 修正：車主密碼改用 .env，不寫死在原始碼

- **做了什麼**：第一次 commit 時被 Claude Code 的自動安全機制擋下（判定為「Credential Leakage」，因為 `OWNER_PASSWORD` 的預設值直接寫死在 `app.py` 原始碼裡）。改為：`OWNER_PASSWORD` 不再有寫死的預設值（未設定時一律拒絕車主登入並提示尚未設定）；新增 `python-dotenv` 依賴，本機建立不會進 git 的 `.env` 檔提供密碼；已重新在瀏覽器驗證本機登入正常運作
- **為什麼**：即使是內部小工具用的簡單密碼，把密碼字串直接寫進會進 git 的原始碼仍是不好的習慣（尤其這個 repo 雖是 private，但密碼未來若要換、或不小心公開，寫死在程式碼裡最難處理）
- **影響檔案**：`app.py`, `requirements.txt`, `.env`（新增，不進 git）
- **Git**：✅ 已 commit `b032b8b`
- **文件同步**：✅ 已同步（SDD.md 更新環境變數表與說明）

## 2026-09-24 01:30 — 新增請假/居家狀態標示 + 車主登入密碼

- **做了什麼**：
  - 新增 `MemberDayStatus` 資料表（`member_id`, `record_date`, `status`），記錄成員某天的「請假」或「居家」狀態，跟共乘打卡紀錄互不影響、可同時存在
  - 這個狀態**團隊 5 人互相可見**（跟共乘打卡紀錄「自己只能看自己的、車主看全部」不同的隱私規則），因此把「查看成員」下拉選單從車主專用改成所有人都能切換查看；但共乘打卡明細與金額仍只有本人與車主能看到（新增 `can_view_rides` 權限判斷，非本人且非車主時共乘相關資料在後端就不會撈出，不只是前端隱藏）
  - 共乘日曆點選日期的面板新增「請假/居家狀態」下拉選單（正常／請假／居家），儲存後日曆格子會標色：請假＝紫色、居家＝黃色（原本國定假日已用粉紅色，故未沿用粉紅避免混淆）
  - 車主（Hugo）登入新增密碼保護（密碼透過環境變數 `OWNER_PASSWORD` 設定；本機用 `.env` 檔提供，雲端在 Render 環境變數設定，程式碼內沒有寫死密碼），其他 4 位一般成員維持原本點名字即可登入、不需密碼
  - 已在瀏覽器實測：密碼錯誤擋下、正確密碼登入、Hugo 標記請假且同時勾選共乘互不影響、Tina 查看 Hugo 時能看到請假狀態但看不到共乘明細、車主查看他人時仍可看到完整明細
- **為什麼**：使用者希望請假/居家狀態能讓大家提前看到、避免車主白等；並希望車主帳號因為權限較大（可看全部人共乘明細、管理成員）而多一層密碼保護
- **影響檔案**：`models.py`, `services.py`, `app.py`, `templates/login.html`, `templates/dashboard.html`, `templates/base.html`
- **Git**：✅ 已 commit `b032b8b`
- **文件同步**：✅ 已於下一筆同步（PRD.md、SDD.md 都已更新）

## 2026-09-24 01:45 — 同步 PRD/SDD：請假/居家狀態、車主密碼

- **做了什麼**：
  - PRD.md：更新 FR-001（車主登入需密碼）、FR-003（隱私規則加註請假/居家例外）；新增 FR-011（請假/居家狀態標示）與 US-011；更新 5.2 安全需求、10.1 假設、12 專案範圍外、8.1 資料實體，文件版本 1.1 → 1.2
  - SDD.md：§3 補上 `MemberDayStatus` 模型與 `set_day_status()`/`get_month_day_status()`、`app.py` 的權限判斷（`can_edit`/`can_view_rides`/車主密碼比對）；§4.2 新增 `member_day_status` 資料表；§5 更新頁面結構、視覺風格（補紫/黃色）、核心互動流程（查看成員開放給所有人、日曆標色優先順序、面板依權限顯示）；§6 API 新增 `/dashboard/status/save`、更新登入路由說明；§7 新增密碼錯誤與隱私阻擋兩列錯誤處理，文件版本 1.4 → 1.5，對應 PRD 版本更新為 1.2
- **為什麼**：讓文件反映最新的請假/居家狀態功能與車主密碼保護機制
- **影響檔案**：`PRD.md`, `SDD.md`
- **Git**：✅ 已 commit `b032b8b`
- **文件同步**：✅ 本筆即為文件同步

## 2026-09-24 01:00 — 更新 HANDOFF.md 反映雲端部署現況

- **做了什麼**：更新交接文件——加上雲端網址與 GitHub repo 連結、8 個 commit 的完整記錄、「怎麼使用」改為區分雲端版（給其他成員）與本機版（開發測試）、技術棧表格補上 `Procfile`／環境變數說明、已知風險補上 Neon 容量與 Render 睡眠的限制說明
- **為什麼**：先前的 HANDOFF.md 是部署前寫的，雲端部署、GitHub、容量提醒等後續進展都還沒反映進去
- **影響檔案**：`HANDOFF.md`
- **Git**：✅ 已 commit `cb5b8d5`
- **文件同步**：➖ 不適用

## 2026-09-24 00:40 — 同步 SDD 容量監控章節

- **做了什麼**：SDD.md 新增「§8.3 資料庫容量監控」說明 `get_storage_warning()` 機制與限制，§3 `database.py` 職責補上該函數，文件版本 1.3 → 1.4
- **為什麼**：讓 SDD 反映新增的容量提醒功能
- **影響檔案**：`SDD.md`
- **Git**：✅ 已 commit `c1ab676`
- **文件同步**：✅ 本筆即為文件同步

## 2026-09-24 00:30 — 新增 Neon 資料庫容量不足提醒

- **做了什麼**：`database.py` 新增 `get_storage_warning()`，透過 `pg_database_size()` 查詢目前 PostgreSQL 用量，剩餘容量低於 0.1GB（Neon 免費方案上限 0.5GB）時回傳提醒文字；只在雲端連 PostgreSQL 時檢查，本機 SQLite 不受影響。`app.py` 的 `inject_common()` 每次請求都會檢查，`base.html` 新增紅色警示樣式的 banner 顯示。已用真實 Neon 連線驗證（目前用量極小，不觸發）與模擬低容量情境（訊息正確產生）
- **為什麼**：使用者擔心 Neon 免費方案 0.5GB 用滿的風險，希望容量吃緊時能主動提醒清理舊資料（不自動刪除，只提醒）
- **影響檔案**：`database.py`, `app.py`, `templates/base.html`
- **Git**：✅ 已 commit `c1ab676`
- **文件同步**：✅ 已於下一筆同步（SDD.md 新增「§8.3 資料庫容量監控」）
- **備註**：以 5 人團體目前的資料量估算，實際觸發這個警示大概需要數百年，這個機制幾乎純粹是安全網，非急迫風險

## 2026-09-24 00:10 — 完成雲端部署驗證 + 同步 SDD 部署章節

- **做了什麼**：
  - 部署驗證：登入頁、儀表板、共乘登記（含中文備註）都在 https://carpool-purchase-tracker.onrender.com/ 實測通過，資料正確寫入/讀出 Neon PostgreSQL；測試留下的 2 筆測試紀錄已清除，正式資料庫恢復乾淨
  - SDD.md：新增「§8 部署與運維」章節（本機開發／雲端部署兩種模式、GitHub+Render+Neon 的角色分工、環境變數表、免費方案限制），更新 §1.3 技術選型、§3 `database.py` 職責、§4.1 資料庫選型、§9（原§8）實作路徑階段 1/6 反映 `gunicorn`/`psycopg2-binary`/`Procfile`，文件版本 1.2 → 1.3
- **為什麼**：讓 SDD 反映系統現在支援雲端多人使用的部署方式，不只是本機單機工具
- **影響檔案**：`SDD.md`
- **Git**：✅ 已 commit `fc4d5dc`
- **文件同步**：✅ 本筆即為文件同步

## 2026-09-23 23:30 — 準備雲端部署（Render + 外部永久 PostgreSQL）

- **做了什麼**：
  - `database.py`：改為優先讀取環境變數 `DATABASE_URL`（有設定就接雲端 PostgreSQL，自動把 `postgres://` 轉成 SQLAlchemy 需要的 `postgresql://`）；沒有設定時（本機開發）沿用原本的本機 SQLite 檔案，行為不變
  - `app.py`：`secret_key` 改為可用環境變數 `SECRET_KEY` 覆蓋（本機沒設定時沿用原預設值）；`init_db()` 改成在模組載入時就執行（不只在 `python app.py` 直接執行時才跑），因為部署到 Render 後是用 gunicorn 載入 `app` 模組，不會經過 `if __name__ == "__main__"` 那段
  - `requirements.txt`：新增 `gunicorn`（正式環境用的 WSGI 伺服器，取代 Flask 內建的開發用伺服器）、`psycopg2-binary`（連接 PostgreSQL 需要的驅動）
  - 新增 `Procfile`，告訴 Render 用 `gunicorn app:app --bind 0.0.0.0:$PORT` 啟動
  - 已在本機驗證：改動後本機仍照舊用本機 SQLite 正常運作，既有的真實成員資料不受影響
- **為什麼**：使用者要把系統分享給其他 4 位不一定在同一個網路環境的成員使用，需要部署到大家都連得到的地方。查證後發現 Render 免費方案的 Web Service 硬碟不持久（重啟/重新部署就清空）、免費 PostgreSQL 30 天會過期，因此改接外部免費且不過期的 PostgreSQL（Neon）來保存資料
- **影響檔案**：`database.py`, `app.py`, `requirements.txt`, `Procfile`（新增）
- **Git**：✅ 已 commit `8924bfb`
- **文件同步**：✅ 已於下一筆同步（SDD.md 補上「§8 部署與運維」章節）

## 2026-09-23 23:00 — 新增 HANDOFF.md 交接文件

- **做了什麼**：建立 `HANDOFF.md`，整理專案現況（成員名單、啟動方式、技術棧、已知風險與限制、下一步方向），供未來接手或開新對話時快速接續
- **為什麼**：使用者要求整理一份 handoff 文件存到專案資料夾
- **影響檔案**：`HANDOFF.md`
- **Git**：✅ 已 commit `f130d07`
- **文件同步**：➖ 不適用

## 2026-09-23 22:40 — 同步 PRD/SDD 至最新功能（備註、代買可留空、假日標示）

- **做了什麼**：
  - PRD.md：新增 FR-009（紀錄備註）、FR-010（國定假日標示）與對應用戶故事 US-009/US-010；更新 FR-004 與 UC-002 反映「分攤對象可留空」；補充 8.1 資料實體（備註欄位、國定假日資料），文件版本 1.0 → 1.1
  - SDD.md：新增 `holidays.py` 模組說明、§4.4 假日資料檔案儲存設計、§5.3 假日粉色標示與缺資料提示的互動流程、§6 API 對應函數、§7 錯誤處理新增一列，對應 PRD 版本更新為 1.1，文件版本 1.1 → 1.2
- **為什麼**：讓 PRD/SDD 反映目前系統已實作的完整功能範圍，避免文件與程式碼脫節
- **影響檔案**：`PRD.md`, `SDD.md`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：✅ 本筆即為文件同步

## 2026-09-23 22:20 — 清除孤立測試紀錄

- **做了什麼**：檢查資料庫發現 4 筆共乘紀錄、1 筆代買紀錄（含 1 品項、3 筆分攤）是使用者測試階段用已刪除的「成員A」「成員B」建立的，成員刪除後這些紀錄變成孤兒資料（`member_id`/`initiator_id` 指向不存在的成員，畫面顯示「代買人：」「分攤人：」空白）。使用者確認這些是測試資料，直接刪除，資料庫恢復乾淨狀態（5 位真實成員、0 筆共乘/代買紀錄）
- **為什麼**：避免孤立資料讓對帳畫面顯示空白姓名造成混淆
- **影響檔案**：`carpool_purchase.db`（資料清理，非結構變更）
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用
- **發現的潛在問題（尚未修正，供之後決定）**：目前「成員管理」刪除成員時，不會檢查/處理該成員名下已有的共乘或代買紀錄，會產生同樣的孤兒資料問題。是否要之後補上「刪除前先警告有歷史紀錄」或「禁止刪除有歷史紀錄的成員」，待使用者決定。

## 2026-09-23 22:00 — 無假日資料年度顯示提示訊息

- **做了什麼**：新增 `holidays.has_holiday_data(year)`，當查看的年度沒有對應的假日資料檔時，在共乘日曆區塊與點選日期後的面板都顯示提示「尚未有 {{年度}} 年度政府行政機關辦公日曆表資料更新」；已用 2028 年（尚無資料）與 2026/2027 年（有資料）分別驗證正確顯示/不顯示
- **為什麼**：使用者希望日後遇到沒有假日資料的年度時，畫面能明確告知而不是靜默不顯示，避免誤以為「這天沒有假日」
- **影響檔案**：`holidays.py`, `app.py`, `templates/dashboard.html`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：⏸ 未同步（與假日標示功能一起，待補進 PRD/SDD）

## 2026-09-23 21:45 — 補上 2027 年（116年）假日資料

- **做了什麼**：使用者提供官方公告網址，下載 116 年中華民國政府行政機關辦公日曆表 CSV，轉換並新增 `data/holidays/2027.json`（24 筆假日/補假），瀏覽器實測 2027/1/1 開國紀念日正確標示粉色
- **為什麼**：延續假日標示功能到下一個年度，避免跨年後日曆失去假日標示
- **影響檔案**：`data/holidays/2027.json`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用（資料檔更新，非功能/架構變更）

## 2026-09-23 21:30 — 佔位成員改為真實姓名＋共乘日曆套用政府行政機關辦公日曆表

- **做了什麼**：
  - 直接更新既有資料庫中的成員姓名（保留原本 id 與歷史紀錄關聯，非刪除重建）：車主→Hugo、成員C→Mango、成員D→Rennie；Tina、Blue（使用者先前已自行新增）維持不變
  - 同步更新 `database.py` 的 `INITIAL_MEMBERS` 種子名單，避免日後資料庫重建又跑出舊佔位名稱
  - 新增 `holidays.py` 與 `data/holidays/2026.json`：從行政院人事行政總處公告的「115年中華民國政府行政機關辦公日曆表」官方 CSV 下載並整理出全年 22 個有名稱的假日/補假日期，共乘日曆對應日期會以粉色標示（含 hover 提示假日名稱），並在圖例加上「國定假日」說明
  - 更新 `README.md` 成員名單與改名說明
- **為什麼**：使用者要求把佔位成員換成實際共乘團體成員，並希望日曆能反映真正的國定假日，方便安排共乘（假日不用共乘上下班）
- **影響檔案**：資料庫 `carpool_purchase.db`（資料更新，非結構變更）、`database.py`、`holidays.py`（新增）、`data/holidays/2026.json`（新增）、`app.py`、`templates/dashboard.html`、`templates/base.html`、`README.md`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：⏸ 未同步（PRD/SDD 都還沒提到「假日標示」這個新功能，待使用者確認是否要補上）
- **備註**：假日資料目前僅涵蓋 2026 年；`holidays.py` 設計為若對應年度沒有資料檔會靜默回傳空字典（不報錯，只是不標示假日），之後跨年需要另外下載/整理新年度的資料檔放到 `data/holidays/<年份>.json`

## 2026-09-23 20:55 — 同步 SDD 至最新程式碼（v1.0 → v1.1）

- **做了什麼**：更新 SDD §1.3/§2.2（新增備註、分攤可留空、儀表板統一敘述）、§3（`services.py` 函數清單改為 `set_carpool_slot`/`get_member_monthly_summary` 等，`database.py` 補充路徑不依賴工作目錄的說明）、§4.2（`note` 欄位、分攤可留空的設計假設更新）、§5（頁面結構改為單一 `/dashboard`、新增視覺風格章節）、§6（API 路由表改為新路由）、§7（移除已不成立的「未選分攤對象」錯誤情境）、§8（roadmap 內函數名稱、port 5000→5050 更正），文件版本 1.0 → 1.1
- **為什麼**：讓 SDD 反映目前實際的整合式儀表板架構，避免文件與程式碼脫節
- **影響檔案**：`SDD.md`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：✅ 本筆即為文件同步

## 2026-09-23 20:40 — 全面改版：深色文青風＋共乘代買整合為單一儀表板

- **做了什麼**：
  - 資料庫新增 `note` 備註欄位（`carpool_records`、`purchase_records`）
  - 放寬代買分攤限制：`share_member_ids` 可為空，代表僅自己記錄、不需要別人付款（原本強制至少選一人）
  - 將原本分開的「共乘紀錄」「新增共乘紀錄（月曆）」「代買清單」「新增代買紀錄」四個頁面整合成單一 `/dashboard` 頁面：頂部本人本月統計卡片（共乘趟數/車資小計/代買小計/應付總計）、含 ISO 8601 週別的共乘日曆（點日期開啟編輯面板：上班/下班切換、備註、付款標記）、代買記錄區（快速新增＋清單＋分攤付款標記）
  - 全站套用深色「簡潔文青蘋果風」視覺（`base.html` 改寫 CSS 變數、配色、卡片與月曆樣式）
  - 修正 `database.py` 用程式檔案自身路徑組資料庫路徑，避免因啟動時工作目錄不同（如透過 `.claude/launch.json` 啟動）而在錯誤位置建立/讀取資料庫
  - 清除因本次改動變成孤立程式碼的 `create_carpool_record`、`delete_carpool_record`、`get_carpool_day_note`（已被 `set_carpool_slot` 取代），改用 `set_carpool_slot`／`get_member_monthly_summary`／`get_month_carpool_records`／`get_month_purchase_records`
  - 已在瀏覽器實測：登入、共乘切換+備註+付款、代買（含分攤與純自己記錄兩種情境）、車主查看他人時的唯讀限制、統計卡片數字正確性
- **為什麼**：使用者提供視覺參考圖，希望介面更貼近實際會長期使用的樣子（深色簡潔風、共乘代買整合成一頁操作、支援備註），並保留原有分攤付款的核心機制
- **影響檔案**：`models.py`, `services.py`, `app.py`, `database.py`, `templates/base.html`, `templates/login.html`（新增 `templates/dashboard.html`；刪除 `templates/carpool.html`, `templates/carpool_new.html`, `templates/purchase.html`, `templates/purchase_new.html`）
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：⏸ 未同步（PRD 的介面相關敘述、SDD §2/§3/§5/§6 都因這次改版有較大落差，待使用者確認是否要一起同步）

## 2026-09-23 19:55 — 新增共乘紀錄改為月曆式介面

- **做了什麼**：把「新增共乘紀錄」頁面從單筆日期表單改為整月月曆檢視（星期一為一週開始、可切換上/下個月），每格顯示當天早上/晚上的勾選框；已登記過的格子改顯示「已登記」文字取代勾選框，避免重複送出；一次可勾選整月多個日期/時段後一次儲存
- **為什麼**：使用者希望登入後選月份、整月呈現、勾選後儲存，比逐筆填單更方便
- **影響檔案**：`app.py`（新增 `_month_weeks()`、改寫 `carpool_new` 路由）、`templates/carpool_new.html`、`templates/base.html`（月曆樣式）
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：⏸ 未同步（SDD §5.1 頁面說明仍寫舊版單筆表單，待使用者確認是否同步更新）

## 2026-09-23 19:35 — 再次修正啟動系統.bat（改用純英文避開編碼問題）

- **做了什麼**：加 UTF-8 BOM 後使用者實測仍亂碼（該電腦的 `cmd.exe` 用系統預設 Big5 編碼讀取批次檔，連 BOM 位元組本身也被誤讀成亂碼字），改為將 `啟動系統.bat` 全部訊息改寫成純英文（純 ASCII，無 BOM），徹底避開編碼問題
- **為什麼**：批次檔的中文編碼在不同 Windows 環境下的行為不一致，最穩妥的方式是不依賴任何特定編碼
- **影響檔案**：`啟動系統.bat`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用

## 2026-09-23 19:20 — 修正啟動系統.bat 中文編碼問題

- **做了什麼**：使用者實際雙擊執行後出現中文亂碼與「不是內部或外部命令」錯誤，改以 UTF-8 with BOM 編碼重新寫入 `啟動系統.bat`
- **為什麼**：原始檔案未帶 BOM，Windows `cmd.exe` 會用系統預設編碼（非 UTF-8）解析檔案內容，導致中文字串與指令本身被誤判為亂碼指令
- **影響檔案**：`啟動系統.bat`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用

## 2026-09-23 19:05 — 新增快速啟動 .bat

- **做了什麼**：建立 `啟動系統.bat`（沿用同工作區 `calendar-notes` 專案的既有風格），雙擊即可啟動 Flask 應用並自動開啟瀏覽器 `http://localhost:5050`
- **為什麼**：方便非技術背景的使用者（5 位團體成員）不需輸入指令就能啟動系統
- **影響檔案**：`啟動系統.bat`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用

## 2026-09-23 18:55 — [TASK-006] 文件與部署（全部任務完成）

- **做了什麼**：建立 `README.md`（啟動方式、成員名單調整說明、文件連結），並依 README 步驟重新啟動系統驗證可正常存取；至此 TASK-001~TASK-006 全部完成
- **為什麼**：完成交付所需的說明文件，讓系統可交付日常使用
- **影響檔案**：`README.md`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用

## 2026-09-23 18:40 — [TASK-005] 整合測試與錯誤處理驗證

- **做了什麼**：於瀏覽器實測 5 大情境：① 5 位成員登入與權限對應（一般成員看不到成員管理、`/members` 直接存取回 403）② 共乘重複登記阻擋、刪除、標記已付款 ③ 多品項代買（180 元÷3 人=60/人）與分攤人自行標記付款 ④ 車主新增/刪除成員 ⑤ 月結未付款提示隨紀錄狀態即時更新，皆正確運作
- **為什麼**：驗證整個系統符合 PRD 驗收標準與 SDD 錯誤處理策略
- **影響檔案**：無新增檔案（驗證既有功能）
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用

## 2026-09-23 18:10 — [TASK-004] 使用者介面開發

- **做了什麼**：實作 `app.py`（全部路由、登入 Session、權限檢查）與 7 個 Jinja2 Templates（`base`/`login`/`carpool`/`carpool_new`/`purchase`/`purchase_new`/`members`）；於瀏覽器實測登入、共乘新增與付款標記、代買新增與分攤金額計算（100 元÷2人=50/50）、一般成員權限限制（看不到成員管理連結、直接打 `/members` 回 403）皆正確運作
- **為什麼**：完成使用者介面層，串接業務邏輯，讓系統可實際操作使用
- **影響檔案**：`app.py`, `templates/base.html`, `templates/login.html`, `templates/carpool.html`, `templates/carpool_new.html`, `templates/purchase.html`, `templates/purchase_new.html`, `templates/members.html`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用

## 2026-09-23 17:35 — [TASK-003] 核心業務邏輯開發

- **做了什麼**：實作 `services.py` 全部業務函數（共乘登記/刪除/付款標記、代買登記與平均分攤、月結未付款計數、成員新增/刪除），並以已知輸入手動驗證所有情境；過程中修正一個 `PurchaseRecord` 關聯資料在 session 關閉後存取會噴 `DetachedInstanceError` 的 bug（改為在 session 內先觸發載入 `items`/`shares`）
- **為什麼**：完成核心業務邏輯層，供使用者介面呼叫
- **影響檔案**：`services.py`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用

## 2026-09-23 17:15 — [TASK-002] 資料庫模組開發

- **做了什麼**：建立 `models.py`（Member/CarpoolRecord/PurchaseRecord/PurchaseItem/PurchaseShare 五個 SQLAlchemy 模型）與 `database.py`（`init_db()`、`get_session()`，並預先建立 1 車主 + 4 成員的佔位名單），實際執行建表並驗證資料寫入正確
- **為什麼**：完成資料層，供後續業務邏輯與介面模組使用
- **影響檔案**：`models.py`, `database.py`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用

## 2026-09-23 17:00 — [TASK-001] 環境建置與專案初始化

- **做了什麼**：建立 `requirements.txt`（flask、SQLAlchemy）、`.gitignore`、`templates/` 目錄、建立虛擬環境並成功安裝依賴
- **為什麼**：完成專案骨架，供後續資料庫與業務邏輯模組開發
- **影響檔案**：`requirements.txt`, `.gitignore`, `templates/`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用

## 2026-09-23 16:50 — 規劃 TASK.md（6 個開發任務）

- **做了什麼**：依 SDD §8 實作路徑拆解出 6 個開發任務（環境建置、資料庫模組、核心業務邏輯、使用者介面、整合測試、文件與部署），使用者已確認任務清單
- **為什麼**：把 SDD 的實作路徑落地成可追蹤的待辦清單，作為接下來一步步開發的依據
- **影響檔案**：`TASK.md`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用

## 2026-09-23 16:40 — 完成 SDD（精簡版）與技術架構圖

- **做了什麼**：
  - 選定 SDD 精簡版模式，技術棧確認為 Flask + Jinja2 HTML Templates + SQLite + SQLAlchemy
  - 產出 `SDD.md`，含資料表設計（members / carpool_records / purchase_records / purchase_items / purchase_shares）、模組設計、API 路由、實作路徑
  - 從 SDD 反推技術架構圖 `docs/diagrams/technical-architecture.puml`，並成功渲染為 PNG
- **為什麼**：依 PRD 需求完成技術架構設計，作為後續規劃 TASK.md 與實際開發的依據
- **影響檔案**：`SDD.md`, `docs/diagrams/technical-architecture.puml`, `docs/diagrams/technical-architecture.png`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用（本次即為文件首次產出）

## 2026-09-23 16:15 — 完成 PRD 諮詢與初版 PRD、概念架構圖

- **做了什麼**：
  - 依「完整專案模式」與使用者進行需求訪談（共乘規則、代買規則、權限、付款狀態標記、月結算提示、成員管理等）
  - 產出完整版 `PRD.md`（含功能需求、非功能性需求、用戶故事、風險評估等章節）
  - 產出概念架構圖 `docs/diagrams/concept-architecture.puml`，並成功渲染為 PNG
- **為什麼**：建立「共乘與代買明細管理系統」專案的需求基礎文件，作為後續 SDD 架構設計與開發的依據
- **影響檔案**：`PRD.md`, `docs/diagrams/concept-architecture.puml`, `docs/diagrams/concept-architecture.png`
- **Git**：✅ 已 commit `6973f83`
- **文件同步**：➖ 不適用（本次即為文件首次產出）
